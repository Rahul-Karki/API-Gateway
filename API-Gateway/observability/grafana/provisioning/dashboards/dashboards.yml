apiVersion: 1

providers:
  - name: "nginx-gateway"
    orgId: 1
    folder: "API Gateway"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
